import Navbar from '../utils/NavBar';
import { Inflatables } from './AllBoards';

const InflatableBoards = () => {
	return (
		<>
			<Navbar />
			<Inflatables />
		</>
	);
};

export default InflatableBoards;
